let users: string[] = [
  "Ali",
  "Ahmed",
  "Fahad",
  "Asim",
  "Admin",
  "Abdullah",
  "Salman",
];

if (users.length === 0) {
  console.log(" We need to find some users!");
}

users = []; //now users array become empty now if condition will be printed

if (users.length === 0) {
  console.log(" We need to find some users!");
}
